











<button onClick={() => {
  deleteProduct({ variables: {input: {_id, productname,price,category }} });
}}></button>

// <button onClick={() => {
//  addProduct({ variables: {input: {_id, productname,price,category }} });
// }}></button>
